name = "birceflix"
compatibility_date = "2025-05-01"
pages_build_output_dir = "dist"

# Set secrets in the Cloudflare dashboard:
#   Pages → Project → Settings → Environment variables:
#     TMDB_API_KEY (encrypted)
#     OMDB_API_KEY (encrypted)
# For local dev with `wrangler pages dev`, create `.dev.vars` next to this file
# (already in .gitignore) with the same keys.
